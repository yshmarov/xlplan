test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

amazon_production:
  service: S3
  #access_key_id: "AKIAIUGSINPRMF5Y7Y7A"
  #secret_access_key: "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"
  #bucket: "xlplan-active-storage2"
  #access_key_id: "AKIAT7WZE2FUUHDGR265"
  #secret_access_key: "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"
  access_key_id: "AKIA5RINJLYGFFYTDUEH"
  secret_access_key: "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"
  bucket: "xlplan-activestorage-zahoruiko"
  region: "eu-central-1"

# Use rails credentials:edit to set the AWS secrets (as aws:access_key_id|secret_access_key)

# amazon:
#   service: S3
#   access_key_id: <%= Rails.application.credentials.dig(:aws, :access_key_id) %>
#   secret_access_key: <%= Rails.application.credentials.dig(:aws, :secret_access_key) %>
#   region: us-east-1
#   bucket: your_own_bucket

# Remember not to checkin your GCS keyfile to a repository
# google:
#   service: GCS
#   project: your_project
#   credentials: <%= Rails.root.join("path/to/gcs.keyfile") %>
#   bucket: your_own_bucket

# Use rails credentials:edit to set the Azure Storage secret (as azure_storage:storage_access_key)
# microsoft:
#   service: AzureStorage
#   storage_account_name: your_account_name
#   storage_access_key: <%= Rails.application.credentials.dig(:azure_storage, :storage_access_key) %>
#   container: your_container_name

# mirror:
#   service: Mirror
#   primary: local
#   mirrors: [ amazon, google, microsoft ]
