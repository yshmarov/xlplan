.card
  - @comments.each do |comment|
    .card-header
      = comment.created_at.to_s(:long)
      by
      = comment.employee
      .float-right
        = link_to "x", [@commentable, comment], method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-sm btn-danger"
    .card-body
      = comment.content