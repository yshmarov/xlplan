- model_class = Event
.card
  .card-body
    %h4=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => model_class.model_name.human.titleize
    
    = render 'form'
    %p
    = link_to t('.cancel', :default => t("helpers.links.cancel")), @event, :class => 'btn btn-danger'
