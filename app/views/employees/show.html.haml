%p
.row
  .col-lg-4
    .card
      .card-header
        %b Personal
      .card-body
        %p
          %b First name:
          = @employee.first_name
        %p
          %b Middle name:
          = @employee.middle_name
        %p
          %b Last name:
          = @employee.last_name
        %p
          %b Phone number:
          = link_to "tel:#{@employee.phone_number}", class: "button" do
            #{@employee.phone_number}
          /%a{:href => "viber://chat?number=#{@employee.phone_number}"}
          /  Viber
        %p
          %b Email:
          = @employee.email
        %p
          %b Date of birth:
          = @employee.date_of_birth.strftime('%d/%b/%Y') if @employee.date_of_birth.present?
          (#{@employee.age})
        %p
          %b Sex:
          = @employee.sex
        %p
          %b Address:
          = @employee.address
    %p
    .card
      .card-header
        %b Employment
        .float-right
          = link_to 'Edit', edit_employee_path(@employee), class: 'btn btn-sm btn-warning'
          = link_to 'Back', employees_path, class: 'btn btn-sm btn-secondary'
      .card-body
        %p
          %b Balance:
          = number_to_currency(@employee.balance_cents, :unit => "$")
        %p
          %b Status:
          = status_label(@employee.status)
        %p
          %b Location:
          = link_to @employee.location, location_path(@employee.location)
        %p
          %b Employment date:
          = @employee.employment_date.strftime('%d/%b/%Y') if @employee.employment_date.present?
        %p
          %b Termination date:
          = @employee.termination_date.strftime('%d/%b/%Y') if @employee.termination_date.present?
    %p
    .card
      .card-header
        %b Skills
      .card-body
        = @employee.skills.map{|p| p.service_category}.join(", ")
        = link_to 'Add Skill', new_skill_path(employee_id: @employee.id)
    %p
    .card
      .card-header
        %b Account & access
      .card-body
        %p
          %b ?
  .col-lg-8
    %ul#myTab.nav.nav-tabs{:role => "tablist"}
      %li.nav-item
        %a#home-tab.nav-link.active{"aria-controls" => "home", "aria-selected" => "true", "data-toggle" => "tab", :href => "#jobs", :role => "tab"} Jobs
      %li.nav-item
        %a#profile-tab.nav-link{"aria-controls" => "profile", "aria-selected" => "false", "data-toggle" => "tab", :href => "#categories", :role => "tab"} Categories
      %li.nav-item
        %a#contact-tab.nav-link{"aria-controls" => "contact", "aria-selected" => "false", "data-toggle" => "tab", :href => "#stats", :role => "tab"} Stats
    #myTabContent.tab-content
      #jobs.tab-pane.fade.show.active{"aria-labelledby" => "home-tab", :role => "tabpanel"}
        = render 'jobs/index'
      #categories.tab-pane.fade{"aria-labelledby" => "profile-tab", :role => "tabpanel"}
        /= render 'employee_service_categories/index'
      #stats.tab-pane.fade{"aria-labelledby" => "contact-tab", :role => "tabpanel"}
        .card-header
          %i.fa.fa-calendar
          /= Job.where(employee_id: current_user.person.employee.id).count
          = @employee.jobs_count
          %b Jobs registered
        .card-header
          %i.fa.fa-clock
          = @employee.jobs.map(&:service_duration).sum/60.to_d
          %b Hours worked
        .card-header
          %i.fa.fa-dollar-sign
          = number_to_currency(@employee_total_earnings, :unit => "USD, ")
          %b My earnings

