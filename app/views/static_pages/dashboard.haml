%h3.text-center
  My stats
.row
  .col-md-3
    %button.btn.btn-info.btn-block{:style => "cursor:default;"}
      %i.fa.fa-calendar
      /= Job.where(employee_id: current_user.person.employee.id).count
      = current_user.person.employee.jobs_count
      %div 
        %b My Jobs
  .col-md-3
    %button.btn.btn-info.btn-block{:style => "cursor:default;"}
      %i.fa.fa-dollar
      = number_to_currency(Job.where(employee_id: current_user.person.employee.id).map(&:employee_price_cents).sum, :unit => "USD, ")
      %div 
        %b My earnings
  .col-md-3
    %button.btn.btn-info.btn-block{:style => "cursor:default;"}
      %i.fa.fa-dollar
      = (Job.where(employee_id: current_user.person.employee.id).map(&:service_duration).sum)/60.to_d
      %div 
        %b Hours worked
%br
%h3.text-center
  Total stats
.row
  .col-md-3
    %button.btn.btn-info.btn-block{:style => "cursor:default;"}
      %i.fa.fa-user
      = Client.count
      %div 
        %b Clients
  .col-md-3
    %button.btn.btn-info.btn-block{:style => "cursor:default;"}
      %i.fa.fa-calendar
      = Job.count
      %div 
        %b Jobs
  .col-md-3
    %button.btn.btn-info.btn-block{:style => "cursor:default;"}
      %i.fa.fa-dollar
      = number_to_currency(Job.all.map(&:client_price_cents).sum, :unit => "USD, ")
      %div 
        %b Earnings
  .col-md-3
    %button.btn.btn-info.btn-block{:style => "cursor:default;"}
      %i.fa.fa-dollar
      = number_to_currency(Job.all.map(&:employee_price_cents).sum, :unit => "USD, ")
      %div 
        %b Expences
%br
.row
  .col-md-6
    %h3.text-center
      Upcoming birthdays
    %table.table-condensed.table
      %tbody
        - @people.each do |person|
          %tr
            %td.list-group-item.list-group-item-warning
              = person.date_of_birth.strftime('%b, %d')
              = link_to person, person_path(person)

/Jobs 
/Jobs total today
/Jobs this month
/Jobs total past - events, earnings
/
/Earnings
/this year
/total
/Planned earning today
/
/Categories/Services pie chart (last 3 months)
/
/ADD <PERIODICITY> for Service - how often is a client expected to use it?
/
/dashboard
/calls
/schedule
/follow up
/search
/reports
/help
/
/
/rename events to jobs?
/invoice?



