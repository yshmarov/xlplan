.row
  .col-lg-3
  .col-lg-6
    .card
      .card-body
        %h5
          .far.fa-address-book
          Select member
        %hr

        = render layout: 'form' do |f|
          = f.input :email
        
        %hr
        = link_to 'Back', previous_wizard_path, class: 'btn btn-secondary'
        = link_to 'Skip', next_wizard_path, class: 'btn btn-secondary'
        = link_to 'Any member', wizard_path(:personal_data), class: 'btn btn-primary'
