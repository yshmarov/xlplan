- model_class = InboundPayment
.card
  .card-body
    %h5
      .fa.fa-dollar-sign
      =t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => model_class.model_name.human.titleize
    %hr
    = render 'form'
    %p
    = link_to t('.cancel', :default => t("helpers.links.cancel")), @inbound_payment, :class => 'btn btn-danger'
